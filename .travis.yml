sudo: false

language: scala

jdk:
  - openjdk8

env:
  - "IDEA_VERSION=201.5616.10;PHP_PLUGIN_VERSION=201.5616.10"

script:
   - sbt -DIDEA_VERSION=$IDEA_VERSION -DPHP_PLUGIN_VERSION=$PHP_PLUGIN_VERSION "; release"

matrix:
  allow_failures: []
